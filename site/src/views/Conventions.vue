<template><article class="doc-section"><span class="doc-eyebrow">FOUNDATIONS</span><h1>通用约定</h1><p>先了解值、尺寸、颜色与表单之间的关系，让组件组合保持一致。</p><h2>全局配置</h2><p>使用 <RouterLink to="/config-provider">ConfigProvider</RouterLink> 统一设置 size、theme（light / dark）和 borderRadius。支持嵌套、动态切换，挂载到 body 的浮层也会继承主题和圆角。</p><h2>尺寸与密度</h2><p>多数组件支持 mini、small、default、medium、large。表单输入控件未显式设置 size 时继承 FormItem / Form 的尺寸。DatePicker 和 DateTimePicker 的 size 同时影响输入框和弹出面板。</p><div class="doc-callout">Drawer 的 width / height 控制面板宽高，size 与 Modal 一样控制密度并传递给内部控件。Icon 使用独立的 number / CSS 长度，默认 1em；Alert 使用固定默认密度。</div><h2>颜色与校验状态</h2><p>Button、Tag、日期选择器及分页等支持 default、primary、success、warning、danger、info 主题。Input、Password 的 status 表示 default、success、warning、error 校验状态。不同组件的 status 含义并不完全相同。</p><p>FormItem 的校验错误由表单规则控制。日期组件的 danger 是红色主题，不会自动令表单校验失败。</p><h2>双向绑定与空值</h2><div class="table-scroll"><table class="doc-table"><thead><tr><th>组件</th><th>值与空值</th></tr></thead><tbody><tr><td>Input / Password</td><td>文本通常使用 string，空字符串表示未输入。</td></tr><tr><td>InputNumber</td><td>number | null，清空使用 null。</td></tr><tr><td>Select</td><td>单选为选项值或 null，多选为值数组；没有匹配项时显示占位提示。</td></tr><tr><td>DatePicker</td><td>YYYY-MM-DD；范围是两个字符串的数组；清空返回 null。</td></tr><tr><td>DateTimePicker</td><td>YYYY-MM-DD HH:mm:ss；本地日历值，不进行时区转换。</td></tr><tr><td>Pagination</td><td>v-model:current-page 与 v-model:page-size 分别控制页码和每页数量。</td></tr></tbody></table></div><h2>表单校验</h2><p>在 Form 上传入 model 与 rules，为 FormItem 设置对应 prop。通过 ref 调用 validate() 后再提交；resetFields() 恢复字段初始值，clearValidate() 仅清理校验状态。</p><h2>键盘与无障碍</h2><p>为没有可见标签的输入提供 aria-label。使用 Tab 移动焦点；日期选择器可通过 Enter、空格或方向下键展开，日历内使用方向键移动、Enter 选择、Escape 取消。不要只依赖颜色传达状态。</p><p>Modal 和 Drawer 支持自动聚焦、焦点限制及关闭后焦点恢复；嵌套弹层优先处理最上层交互。</p><h2>节假日数据</h2><p>日期组件通过 holidays 接收 { key: 'YYYY-MM-DD', value: '节日名' } 数组，showHolidays 控制显示。节假日标记不会自动禁用日期；业务不可选规则应通过 disabledDate 单独设置。</p></article></template>
