<script setup lang="ts">
import { foundations } from '../docs/catalog'
const statusColors=[['default','中性'],['primary','主要'],['success','成功'],['warning','警告'],['danger','危险'],['info','信息']]
</script>
<template><article class="doc-section foundations-page"><div class="page-kicker">DESIGN FOUNDATION / 设计基础</div><h1>先建立一致的规则，<br>再组合组件。</h1><p class="page-lead">Zt UI 的外观和交互由六项基础约定共同决定。Header 中的设置会实时作用于整站，便于验证不同密度、主题和圆角下的真实效果。</p><div class="foundation-index"><a v-for="(item,index) in foundations" :key="item.id" :href="`#${item.id}`"><span>0{{index+1}}</span><strong>{{item.title}}</strong><small>{{item.description}}</small></a></div><section id="theme"><h2>主题 Theme</h2><p>light 与 dark 使用同一套语义变量。组件不直接假设页面背景色，浮层、表单和反馈颜色会跟随最近的 ConfigProvider。</p><div class="surface-demo"><div><span>Surface</span><b>主要内容表面</b></div><div><span>Muted</span><b>辅助和分组表面</b></div><div><span>Elevated</span><b>浮层与弹窗表面</b></div></div></section><section id="density"><h2>密度 Density</h2><p>mini、small、default、medium、large 五档尺寸同时控制字号、控件高度和页面示例留白。default 是业务系统的默认基准。</p><div class="density-scale"><span v-for="size in ['mini','small','default','medium','large']" :key="size" :class="`is-${size}`">{{size}}</span></div></section><section id="radius"><h2>圆角 Radius</h2><p>borderRadius 是形状基准。普通容器跟随基准，圆形头像、状态点和胶囊控件保持自身语义形状。</p><div class="radius-demo"><span v-for="radius in [0,4,8,12,18]" :key="radius" :style="{borderRadius:`${radius}px`}">{{radius}}</span></div></section><section id="status"><h2>状态色 Status</h2><p>颜色只承担辅助识别，组件仍使用文字、图标和语义属性表达状态。</p><div class="status-grid"><div v-for="item in statusColors" :key="item[0]" :class="`is-${item[0]}`"><i/><strong>{{item[0]}}</strong><span>{{item[1]}}</span></div></div></section><section id="accessibility"><h2>可访问性 Accessibility</h2><ul class="doc-points"><li>全部交互可通过键盘到达，并提供清晰、克制的焦点样式。</li><li>无可见标签的输入必须提供 aria-label 或 aria-labelledby。</li><li>弹层恢复焦点，复杂控件使用正确的 role、状态和键盘模型。</li><li>动画遵循 prefers-reduced-motion，状态不只依赖颜色。</li></ul></section><section id="responsive"><h2>响应式 Responsive</h2><p>组件先保证容器内适应，再为移动端调整交互。日期范围切换为纵向双面板，表格保留横向滚动，文档导航在窄屏进入抽屉。</p></section></article></template>
